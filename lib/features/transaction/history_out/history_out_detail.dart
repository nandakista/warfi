import 'package:flutter/material.dart';

class HistoryOutDetail extends StatelessWidget {
  static const String route = '/history/out/detail';
  const HistoryOutDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
